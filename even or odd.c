#include <stdio.h>
int main()
{
    int number;
    printf("Enter an value: ");
    scanf("%d", &number);
    if(number % 2 == 0);
        printf("%d is even.", number);
    else if
        printf("%d is odd.",number);
    else
        printf("%d is invalid.",number);

    return 0;
}
